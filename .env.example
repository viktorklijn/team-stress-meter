# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/team_stress_monitor

# PostgreSQL Configuration (for docker-compose)
POSTGRES_PASSWORD=password

# Application Configuration
NODE_ENV=production
PORT=5000